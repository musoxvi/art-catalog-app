import {StyleSheet} from 'react-native';
import {Spacing} from './spacing';

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: Spacing.space16H,
  },
  flex1: {
    flex: 1,
  },
});
